CREATE TABLE `car_colors` (
    `car_color_id` INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    `car_color` VARCHAR(255)
);


INSERT INTO `car_colors` (`car_color`) VALUES
  ('Черный'),
  ('Белый'),
  ('Красный');
